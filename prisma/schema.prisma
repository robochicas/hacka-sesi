datasource db {
  provider = "postgresql"
  url      = env("DATABASE_CONNECTION_URI")
}

generator client {
  provider = "prisma-client-js"
}


model Consulta{
    id        Int      @id @default(autoincrement())
    data      DateTime
}